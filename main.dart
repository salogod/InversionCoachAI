import 'package:flutter/material.dart';
import 'package:iconn_app/inicio.dart';
import 'crear_cuenta.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({Key? key});

  @override
  Widget build(BuildContext context) {
    return inicio();
    
      
    
  }
}